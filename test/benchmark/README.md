# T5 Benchmark regression

Latency, throughput, memory pressure, and power benchmarks. Result artifacts
are published per run; pass/fail thresholds are reviewed per release.

Gated to release branches. Not required for ordinary PRs.
