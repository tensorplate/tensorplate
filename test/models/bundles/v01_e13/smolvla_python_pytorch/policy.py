# tensorplate v0.1 synthetic Python sidecar entry placeholder.
#
# This is NOT the SmolVLA policy. The on-device runtime path uses the real
# `tensorplate-backend-python-pytorch` SmolVLA implementation; this fixture
# exists for parser / verifier conformance only and ships with a
# deterministic digest.

ENTRY = "smolvla.policy:Policy"
