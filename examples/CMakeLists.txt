# Developer-facing runnable examples. These exercise the public C++
# runtime API end-to-end so a clean build can be verified without the
# serving worker or external clients. They are NOT part of the v0.1.0
# product surface and are off by default.

add_subdirectory(buffer_plane)
