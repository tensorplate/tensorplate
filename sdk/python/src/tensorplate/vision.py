"""High-level vision detection client built on top of the serving client."""

from __future__ import annotations


class VisionClient:
    """High-level object-detection client.

    Placeholder: image preprocessing, the ``detect`` path, and detector
    postprocessing are not yet implemented. The client composes the
    serving client with client-side pre/post helpers.
    """
